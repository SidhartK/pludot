// Text is the base type for all pludot unstructured text types.
// Rust-side logic (e.g. expressions operating on Text columns) wil live here.
// The dtype itself is defined on the Python side via pl.Object